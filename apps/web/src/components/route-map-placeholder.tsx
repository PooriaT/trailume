export function RouteMapPlaceholder() {
  return (
    <section className="card">
      <h3>Map (OSM-ready)</h3>
      <p>
        Route visualization will use OpenStreetMap tiles via React Leaflet once Strava polylines are wired.
      </p>
    </section>
  );
}
